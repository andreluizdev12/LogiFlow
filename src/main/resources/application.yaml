spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/logiFglow
    username: postgres
    password: Aqws0897
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update # create | create-drop | validate | update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  sql:
    init:
      mode: never